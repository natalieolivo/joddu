import React from "react";
import styled from "styled-components";

const ThemeManager = () => {
  return (
    <h1>
      This page will use styled component attributes to set up the theme loaded
      and hit an api endpoint to retain theme state
    </h1>
  );
};

export default ThemeManager;
